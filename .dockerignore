# none of these are COPYed into any image, and game-source alone is ~4.4GB of build context
.git/
.env
game-source/
server-data/
tavern-config/
# the panel image builds these itself inside the container
panel/node_modules/
panel/dist/
panel/dist-server/
